/*type IPost = {
  id: number;
  title: string;
  content: string;
};

const PostCard = ({ id, title, content }: IPost) => {
  return (
    <article>
      <h2>{title}</h2>
      <p>{content}</p>
    </article>
  );
};

export { PostCard };*/


